import CUBE_COLORS from "./cubeColours";

const { RED, ORANGE, BLUE, GREEN, WHITE, YELLOW } = CUBE_COLORS;

// Standard color scheme: U=White, D=Yellow, F=Green, B=Blue, R=Red, L=Orange
const FACE_COLORS = {
  F: GREEN,
  B: BLUE,
  L: ORANGE,
  R: RED,
  U: WHITE,
  D: YELLOW,
};

export default FACE_COLORS;
